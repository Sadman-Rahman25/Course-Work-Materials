1(a)
For this code we took input file after that we read the first line of the input and used
it in the first loop as range then we checked if the function is even or odd after that
we wrote the output file and then we closed it.

1(b)
Here we split the values then we convert the 2nd and 4th values into integer after that we checked the
3rd value and run the function as it was needed and at last we write the output file and closed it afterwards

2
Here for the worst and Avg case scenario the code will run n**2 times but for the best case scenario the code
will run n times cause in the best case scenario before checking the second loop when the value of f would be
0 then the loop will break which will cause the code to run only one loop hence the result would cause n in the
best case scenario

3
Here in k and v i split the values made them into integer and put them in lists. Then I formed a list 'd' with the
id : mark after that i used bubble sort to sort the values and if the values are same then i sorted the k . for
best case scenario i used the break function to avoid the complexity. And at last I used a loop for the output
values

4
Here at first we took the line as the range of the input then we took the inputs. Here we need the 1st and the 
last value so i used the split function to get the values then we did the bubble sort to sort as it was needed 
in the the output 